import 'package:flutter/material.dart';

import 'package:macros_app/widgets/logging_screen/logged_meals_list.dart';

class LoggingScreen extends StatelessWidget {
  const LoggingScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const LoggedMealsList();
  }
}
